# 구로구 AI 마케팅 인턴십 파트너 웹페이지 배포 가이드

이 가이드는 웹페이지를 Google Sheets와 연동하여 신청서 데이터를 자동으로 수집하는 방법을 안내합니다.

## 📋 목차

1. [Google Sheets 생성](#1-google-sheets-생성)
2. [Google Apps Script 설정](#2-google-apps-script-설정)
3. [웹 앱 배포](#3-웹-앱-배포)
4. [HTML 파일 설정](#4-html-파일-설정)
5. [테스트](#5-테스트)
6. [문제 해결](#6-문제-해결)

---

## 1. Google Sheets 생성

### 단계 1: 새 스프레드시트 만들기

1. Google Drive (https://drive.google.com)에 접속합니다
2. **새로 만들기** → **Google Sheets** → **빈 스프레드시트** 클릭
3. 스프레드시트 이름을 **"구로구 AI 마케팅 인턴십 신청서"**로 변경

### 단계 2: 스프레드시트 ID 확인

1. 스프레드시트 URL을 확인합니다
   ```
   https://docs.google.com/spreadsheets/d/[스프레드시트_ID]/edit
   ```
2. `/d/` 다음부터 `/edit` 전까지의 문자열이 **스프레드시트 ID**입니다
3. 이 ID를 복사해 둡니다 (예: `1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p`)

---

## 2. Google Apps Script 설정

### 단계 1: Apps Script 편집기 열기

1. Google Sheets에서 **확장 프로그램** → **Apps Script** 클릭
2. 새 Apps Script 프로젝트가 열립니다

### 단계 2: 코드 복사

1. 기본으로 있는 `function myFunction() {}` 코드를 모두 삭제합니다
2. `google-apps-script.gs` 파일의 내용을 **전체 복사**하여 붙여넣습니다
3. 코드 상단의 **스프레드시트 ID**를 수정합니다:

```javascript
const SPREADSHEET_ID = '여기에_복사한_스프레드시트_ID_붙여넣기';
const SHEET_NAME = '신청서';
```

### 단계 3: 프로젝트 저장

1. 프로젝트 이름을 **"구로구 인턴십 신청서 API"**로 변경
2. **저장** 버튼 (💾) 클릭 또는 `Ctrl+S` (Windows) / `Cmd+S` (Mac)

### 단계 4: 초기 설정 실행 (선택사항)

1. 함수 선택 드롭다운에서 **`setupSpreadsheet`** 선택
2. **실행** 버튼 (▶️) 클릭
3. 권한 요청이 나타나면:
   - **권한 검토** 클릭
   - Google 계정 선택
   - **고급** 클릭 → **"프로젝트명"(으)로 이동** 클릭
   - **허용** 클릭
4. 실행이 완료되면 스프레드시트에 헤더가 자동으로 생성됩니다

---

## 3. 웹 앱 배포

### 단계 1: 배포 시작

1. Apps Script 편집기 우측 상단의 **배포** → **새 배포** 클릭

### 단계 2: 배포 설정

1. **유형 선택** 옆의 ⚙️ (톱니바퀴) 아이콘 클릭
2. **웹 앱** 선택
3. 다음 설정을 입력합니다:
   - **설명**: `구로구 인턴십 신청서 v1.0`
   - **다음 계정으로 실행**: **나**
   - **액세스 권한**: **모든 사용자** (중요!)

### 단계 3: 배포 완료

1. **배포** 버튼 클릭
2. 다시 권한 요청이 나타나면 위와 동일하게 허용합니다
3. **웹 앱 URL**이 표시됩니다 (매우 중요!)
   ```
   https://script.google.com/macros/s/[앱_ID]/exec
   ```
4. 이 URL을 **복사**해 둡니다

---

## 4. HTML 파일 설정

### 단계 1: index.html 수정

1. `index.html` 파일을 텍스트 에디터로 엽니다
2. 파일 상단(94-98줄 근처)의 다음 부분을 찾습니다:

```javascript
const GOOGLE_SCRIPT_URL = 'YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE';
```

3. **복사한 웹 앱 URL**로 교체합니다:

```javascript
const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/[실제_앱_ID]/exec';
```

### 단계 2: 파일 저장

1. 파일을 저장합니다 (`Ctrl+S` 또는 `Cmd+S`)

---

## 5. 테스트

### 단계 1: 로컬에서 테스트

1. `index.html` 파일을 웹 브라우저로 엽니다
2. 신청서 양식을 작성합니다:
   - 회사명: 테스트 회사
   - 담당자: 홍길동
   - 연락처: 010-1234-5678
   - 이메일: test@test.com
   - 업종: IT/소프트웨어
   - 희망 인턴 수: 1명
   - 정규직 전환 의향: ✅ 체크

### 단계 2: 제출 테스트

1. **신청서 제출하기** 버튼 클릭
2. 로딩 애니메이션이 표시됩니다
3. 성공 메시지 모달이 나타나는지 확인합니다

### 단계 3: 스프레드시트 확인

1. Google Sheets로 돌아갑니다
2. **"신청서"** 시트에 데이터가 추가되었는지 확인합니다
3. 다음 컬럼이 있어야 합니다:
   - 제출일시
   - 회사명
   - 담당자 성함
   - 연락처
   - 이메일
   - 업종
   - 희망 인턴 수
   - 정규직 전환 의향

---

## 6. 문제 해결

### 문제 1: 데이터가 스프레드시트에 저장되지 않음

**해결 방법:**

1. Apps Script 편집기에서 **실행** 탭 확인
2. 에러 메시지가 있는지 확인
3. `SPREADSHEET_ID`가 정확한지 재확인
4. 웹 앱 배포 시 **"모든 사용자"** 권한으로 설정했는지 확인

### 문제 2: CORS 에러 발생

**해결 방법:**

- HTML 파일에서 `mode: 'no-cors'` 설정이 되어 있는지 확인
- 웹 앱 URL이 정확히 입력되었는지 확인

### 문제 3: 권한 에러

**해결 방법:**

1. Apps Script 배포 설정에서:
   - **다음 계정으로 실행**: **나**
   - **액세스 권한**: **모든 사용자**
2. 재배포 후 새 URL로 업데이트

### 문제 4: 웹 앱 URL 변경됨

**해결 방법:**

- 새 배포를 만들 때마다 URL이 변경됩니다
- 기존 배포를 **관리**하여 동일한 URL 유지 가능
- 또는 새 URL을 HTML 파일에 다시 입력

---

## 📊 스프레드시트 데이터 활용 팁

### 자동 알림 설정

1. Google Sheets에서 **도구** → **알림 규칙** 클릭
2. **새 규칙 추가**
3. **사용자가 양식을 제출할 때** 또는 **변경사항이 있을 때** 선택
4. 이메일로 알림 받기 설정

### 데이터 필터링

1. 헤더 행 선택 → **데이터** → **필터 만들기**
2. 각 열의 필터 아이콘으로 데이터 정렬/필터링 가능

### 데이터 내보내기

1. **파일** → **다운로드**
2. Excel (`.xlsx`) 또는 CSV 형식으로 내보내기

---

## 🚀 운영 환경 배포

### 웹 호스팅 옵션

1. **GitHub Pages** (무료)
   - GitHub 리포지토리의 Settings → Pages
   - Source를 `main` 브랜치로 설정

2. **Netlify** (무료)
   - https://netlify.com 가입
   - `index.html` 드래그 앤 드롭으로 배포

3. **Vercel** (무료)
   - https://vercel.com 가입
   - GitHub 리포지토리 연결하여 자동 배포

---

## 📞 지원

문제가 지속되면 다음을 확인하세요:

1. Google Apps Script 실행 로그 확인
2. 브라우저 개발자 도구 (F12) 콘솔 확인
3. 네트워크 탭에서 요청/응답 확인

---

**배포 완료 후에는 이 가이드를 보관하여 향후 수정 시 참고하세요!** ✅
